"""Driftline: the console that introduces, evaluates and manages behavior changes.

Depends on packages/agent (to run the same loop production runs) and
packages/behavior_core (config, versions, tables). Never imports apps/chatbot.
"""
